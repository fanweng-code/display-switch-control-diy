# Copy to .local/config/mac.conf and replace every placeholder with values
# measured on your display. Keep SwitchingEnabled=0 until status is correct.
DISPLAY_SELECTOR='REPLACE_WITH_DISPLAY_NAME'
MAC_VCP='0x00'
PC_VCP='0x00'
M1DDC_DISPLAY_UUID='REPLACE_WITH_UUID'
PC_M1DDC_VALUE='0'
SWITCHING_ENABLED='0'
